import { PagePlaceholder } from '@/components/common/PagePlaceholder';

export function CheckoutPage() {
  return (
    <PagePlaceholder
      phase="Phase 6 — Cart & Wishlist"
      title="Checkout (demo)"
      description="Payment and order tracking are out of scope for this build. In Phase 6 this becomes a cart summary plus an 'Order placed (demo)' confirmation."
    />
  );
}
