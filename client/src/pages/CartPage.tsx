import { PagePlaceholder } from '@/components/common/PagePlaceholder';

export function CartPage() {
  return (
    <PagePlaceholder
      phase="Phase 6 — Cart & Wishlist"
      title="Your cart"
      description="The full-page cart (editable line items, totals, proceed-to-checkout) is built in Phase 6. The slide-in Cart Drawer is already live from the bag icon."
    />
  );
}
