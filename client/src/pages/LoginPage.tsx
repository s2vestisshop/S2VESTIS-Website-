import { PagePlaceholder } from '@/components/common/PagePlaceholder';

export function LoginPage() {
  return (
    <PagePlaceholder
      phase="Phase 7 — Auth"
      title="Sign in"
      description="The login form (JWT httpOnly cookie, redirect-back) is built in Phase 7. Auth thunks and protected-route handling are already in the store."
    />
  );
}
