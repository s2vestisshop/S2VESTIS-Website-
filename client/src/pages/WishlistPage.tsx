import { PagePlaceholder } from '@/components/common/PagePlaceholder';

export function WishlistPage() {
  return (
    <PagePlaceholder
      phase="Phase 6 — Cart & Wishlist"
      title="Wishlist"
      description="The saved-products grid with move-to-cart / remove actions is built in Phase 6. Wishlist state (guest via localStorage, synced on login) is already wired in the store."
    />
  );
}
