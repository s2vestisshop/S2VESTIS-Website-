import { PagePlaceholder } from '@/components/common/PagePlaceholder';

export function AccountPage() {
  return (
    <PagePlaceholder
      phase="Phase 7 — Auth"
      title="Account"
      description="A basic account overview is built in Phase 7 alongside the auth pages."
    />
  );
}
