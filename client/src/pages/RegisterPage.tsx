import { PagePlaceholder } from '@/components/common/PagePlaceholder';

export function RegisterPage() {
  return (
    <PagePlaceholder
      phase="Phase 7 — Auth"
      title="Create account"
      description="The register form is built in Phase 7. On sign-up the guest cart and wishlist merge into the new account automatically."
    />
  );
}
